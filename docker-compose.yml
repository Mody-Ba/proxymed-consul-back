services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: proxymed
      POSTGRES_USER: proxymed
      POSTGRES_PASSWORD: proxymed
    ports:
      - "5432:5432"
    volumes:
      - proxymed_db_data:/var/lib/postgresql/data

volumes:
  proxymed_db_data:
